import React from "react";
import { Instagram, Facebook, Twitter, MapPin, Phone, Mail, Calendar, Award } from "lucide-react";

const Footer: React.FC = () => {
    return <></>;
};

export default Footer;